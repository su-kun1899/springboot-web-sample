spring:
  thymeleaf:
    mode: HTML5
    cache: false
  datasource:
    url: jdbc:h2:file:/tmp/customer
    username: sa
    password:
    jpa:
      hibernate:
        ddl-auto: validate
  h2:
    console:
      enabled: true